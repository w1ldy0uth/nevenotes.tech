apiVersion: v1
kind: Service
metadata:
  name: {{ include "nevenotes-tech.fullname" . }}
  labels:
    {{- include "nevenotes-tech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nevenotes-tech.selectorLabels" . | nindent 4 }}
